import { useEffect } from "react";

export default function Index() {
  useEffect(() => {
    // Since we're using state-based routing in App.tsx, 
    // this page is handled by AppContent component
  }, []);

  return null;
}
